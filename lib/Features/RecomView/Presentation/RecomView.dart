import "package:flutter/material.dart";

import "Widgets/RecomViewBody.dart";

class RecomView extends StatelessWidget {
  const RecomView({super.key});

  @override
  Widget build(BuildContext context) {
    return const RecomViewBody();
  }
}
